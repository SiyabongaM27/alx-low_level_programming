#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two numbers
 *
 * @a: integer parsed
 * @b: integer parsed
 *
 * Return: int
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
